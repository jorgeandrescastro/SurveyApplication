<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Facebook ID</th>
            <th>Nombre</th>
            <th>Estado</th>
            <th>Fecha de Comienzo</th>    
            <th></th>        
        </tr>
    </thead>
    <tbody>
        <?foreach($this->users as $id => $user) { 
            $url = $this->url(array('controller' => 'admin', 'action' => 'view', 'iid' => $user->getId()));
        ?>
            <tr>
                <td><? echo $id; ?></td>
                <td><? echo $user->getFbid(); ?></td>
                <td><? echo $user->getName(); ?></td>
                <td><? if($user->hasFinishedSurvey()) {?>
                    <span class="label label-success">Finalizado</span>
                    <? } else { ?>
                    <span class="label label-warning">Incompleto</span>
                    <? }?>                
                </td>
                <td><? echo $user->getStartDate(true); ?></td>
                <td><a href="<? echo $url; ?>">
                        <button type="button" class="btn btn-primary btn-xs">
                            Ver
                        </button>
                    </a>
                </td>
            </tr>
        <? } ?>
    </tbody>
</table>